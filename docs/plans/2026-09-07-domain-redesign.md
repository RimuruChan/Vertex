# Vertex 域与协作工作流重设计计划

目标：实现 [域与协作目标架构](../10-domains-and-access.md)，覆盖 Server、UI、mock、数据库及 Worker 上下文。此清单表示剩余工作，不是上线声明。

## 当前基线

- P0/P1 基线：签名提交 `9396a84`；之前的签名提交保留，不推送。
- 短编号/兼容路由、正常出题工作台和独立 mock 账号已随 P0/P1 收拢，继续保留。
- Windows 原生 PostgreSQL 测试实例可用；真实库使用独立测试数据。禁止使用 WSL。
- 资源持久层、主要资源 API 与页面已接入域，旧无域地址固定官方域；域治理/group 界面、完整协作与复制交互、最终矩阵仍未完成。

## P0：收拢正在进行的改动

- [x] 核对分支、status、源码和主要差异；查阅 Hydro、Polygon、Codeforces 一手说明。
- [x] 固化目标架构、角色层次、工作流及验收范围。
- [x] 补齐独立 mock 账号的权限行为，移除出题演示切换页；修复短编号引入的 ID 比较问题。
- [x] 题目列表只保留查找/创建/进入；编辑、上传及删除移入详情。
- [x] 核对生成产物差异并完成基线检查；本阶段随域底座一并签名提交。

## P1：域、成员、角色与 group 底座

- [x] init schema：官方域、普通域、成员、角色、group、同域约束和必要审计。
- [x] domain 根包 service/store，领域内 dto/handler/router；定义封闭权限目录与纯策略。
- [x] 域创建/列表/读取/设置、加入/审批/邀请、owner 转让与保护。
- [x] 成员/角色/group 管理；权限不允许越权授予；失效成员即时失去群组治理权限（资源继承在 P2）。
- [x] 注册流程加入官方域；站点管理员与域管理员职责分开。
- [x] 真实 PostgreSQL 与 Ginkgo/Gomega 测试：角色矩阵、私有域不可见、跨域组成员拒绝、owner 不变量、并发写保护。

## P2：全部资源接入域与所有权

- [ ] 所有资源读写、count、筛选、公开编号解析、标签和统计明确作用域。
  - [x] 持久层域条件、域内公开编号及并发分配、同域复合 FK；旧无域资源 API 绑定官方域。
  - [ ] 新域资源 API 与资源 capabilities 一起开放，补完整权限矩阵和前端域上下文。
    - [x] 主要资源的带域路由、生成契约、前端绑定与生产路由隔离测试。
    - [ ] 标签/公告域治理、全部关联/统计读策略和最终矩阵。
- [ ] 题目 owner、用户/组 reader/editor、转让、发布、删除与包访问。
  - [x] 必填 owner、直接/组授权、动态继承、并发转让、域锁下重新授权、元信息/包/构建控制、权限 DTO 和协作 API。
  - [x] 工作副本与显式版本发布分离：可变元信息隔离、排队时封存构建输入、候选不自动上线、revision 核对与不可变发布记录。
  - [x] 练习/比赛/generation 固定发布版本，取消重测保留原版本，引用中的版本/文件保留；显式发布与赛务采用版本界面及官方域 mock。
  - [x] 指定发布版本的独立复制后端、源/目标权限复核、来源记录与文件保留。
  - [ ] 复制与完整协作 UI、多域/group mock。
- [ ] 比赛 owner/编辑协作者、用户/组赛务、参赛资格、报名、澄清、榜单与重测。
  - [x] 比赛 owner/admission、用户/group 多角色授权、有效赛务视图、权限过滤管理列表、协作/转让接口及 DTO；报名、答疑、提交和封榜相关回归。
  - [x] 比赛 jury 重测接口/控制台和观察员只读记录，包含目标授权与 Worker 锁顺序回归。
  - [ ] 完整 group mock、多域比赛编辑工作流和同域关联策略最终验收。
- [ ] 题单 owner/协作和同域条目；题解/讨论继承父资源边界并保留作者编辑权。
  - [x] 题单 owner、用户/group reader/editor、事务内撤权检查、转让、同域条目及按题目权限过滤计数/进度；详情能力与协作面板、独立保存设置/编排。
  - [x] 题解/讨论继承父题目、作者编辑与治理删除分离；防剧透覆盖正文/点赞/讨论，题目及题解同线程回复，比赛普通讨论移除。
  - [ ] 完整多域/group mock、题单/社区域导航与最终矩阵验收。
- [ ] 提交创建、反馈、源码读取、进度、批量操作和个人统计统一域与资源策略。
  - [x] 提交/进度读取重新解析域资源管理权，私题协作访问、独立源码授权、当前比赛反馈策略，批次/单条重测/取消/变更列表的目标授权。
  - [ ] 全部统计、跨域页面、共享内容关联与最终权限矩阵合并验收。
- [ ] 请求体不能覆盖路由域；关键写操作在事务内复核；复合 FK 阻止跨域关系。
- [ ] 明确的跨域题目复制、源权限和独立副本生命周期。
  - [x] 源包复制 + 目标域创建权限、默认未发布副本、独立普通文件、不可变来源与继承说明、事务失败清理、源删除后副本可评测。
  - [ ] 在多域工作台接入复制入口、目标选择、来源面板和 mock，完成浏览器矩阵。
- [ ] API 权限响应、Swag/Orval 生成物和领域文档同步。

## P3：正常产品界面与多视角 mock

- [ ] 域导航/切换/官方默认域，资源、管理、group 页面全部在域上下文内。
  - [x] 主要资源 `/d/{domain}` 导航、官方兼容跳转、域内身份显示、登录返回与失权恢复入口。
  - [x] 域目录/创建/加入、成员/角色/group 设置界面、域所有权和归档恢复；对应 mock 治理规则。
  - [ ] 完整资源协作、复制入口和比赛详情管理工作流，最终移动端及权限矩阵。
- [ ] 题目、比赛、题单、group 的列表和详情工作流；一致的设置/权限/危险操作区域。
- [ ] 服务端 capabilities 驱动入口；保持错误/空/加载状态、草稿、URL、焦点和移动端可用。
- [ ] mock 数据覆盖多个域和独立身份，按同一权限矩阵执行，不回退真实后端。
- [ ] 浏览器验收：角色 × 域切换、创建/编辑/上传/构建、协作授权/撤销、参赛/澄清/封榜、跨域拒绝。

## P4：任务、部署与完成审计

- [ ] 判题/构建任务携带正确域，Worker 不访问数据库，lease/generation fencing 不退化。
- [ ] 原生 Go vet/test、真实 PostgreSQL 集成、前端测试/构建、生成物一致性、正常/mock 隔离。
- [ ] 环境支持时运行完整 Docker/CI/E2E；未执行或间接证据单独列出。
- [ ] 架构/数据库/API/部署/README 与实际实现一致；删除过时的「域以后再做」描述。
- [ ] 完整 diff 与需求逐项审计；只提交已验证阶段，签名且不推送，不输出凭据。

## 验收证据表

| 要求                    | 必须检查的证据                                       | 当前状态                 |
| ----------------------- | ---------------------------------------------------- | ------------------------ |
| 官方域与多域隔离        | seed、所有查询 scope、真实跨域 API/DB 测试           | 未完成                   |
| owner、role、group 权限 | 策略矩阵、事务检查、继承权限撤销、转让保护           | 未完成                   |
| 正常详情工作流          | 列表无编辑/上传动作、工作台实际读写与浏览器交互      | 未完成                   |
| 多身份 mock             | 独立账号/域/报名、角色可见数据与操作、无真实网络回退 | 未完成                   |
| 短地址/比赛视图         | 数字解析、兼容地址、比赛内无社区挂载、布局测量       | 部分完成，待域接入后重验 |
| Worker 与资源副本       | 任务上下文、不可变快照、跨域副本删除/修改隔离        | 未完成                   |
| 验证闭环                | 本计划 P0–P4 对应日志、生成物与最后 diff             | 未完成                   |

每个阶段在完成后记录具体命令与结果。变更方案时同步更新设计和待办，不以调整清单来缩小用户目标。

## 2026-09-07 阶段证据

- Windows 原生 PostgreSQL 16 测试实例，新建独立测试库运行 `go -C server vet ./...`、`go -C server test ./... -count=1`，全部通过（数据库集成实际运行）。
- `domain` 套件 13 项：域与角色策略、官方域及注册、邀请/审批/停用、权限授予边界、owner 与官方域保护、并发转让、跨域 group/FK、成员撤权、HTTP 实际数据库授权与 413。
- 浏览器：管理列表编辑按钮为 0、文件输入为 0；从列表创建临时题目自动到数字详情地址；详情保存限制参数后未保存题面保留；保存题面后模拟构建完成。临时验收题目随后从详情删除。
- 当前 UI 仍使用未加域的资源路由，普通资源权限尚未由 domain 底座接管；这是 P2/P3 的剩余工作，不能据上述测试宣称完整多域隔离完成。
- UI：22 项 Vitest、TypeScript、格式检查和正常/mock 构建通过；生产产物未包含 mock 运行时。Worker vet/test 通过。
- Swag/Orval 已更新；停止 Vite 后重复生成并逐文件比较 SHA-256，内容与文件集合不变。开发预览避免监听构建产物、测试文件与 README，减少无意义整页重载。

## 2026-09-07 P2a：资源作用域底座

- 根资源、标签与多父关联表加入域；编号改为域内、按资源种类分配，旧 UUID 保留。完整 init 在新的原生 PostgreSQL 测试库验证，不升级或清空已有开发库。
- 公共持久层查询、count、详情、更新、删除、出题包、测试数据、比赛澄清、提交进度、重测及变更列表限制域；旧无域 API 在认证后绑定官方域。全站账号治理与系统队列统计明确保留站点管理员范围。
- `domain` 套件扩展到 22 项，新增真实数据库证据：12 个并发编号分配、22 种跨域关联被 FK 拒绝、两域同号解析、管理员/资源作者持外域 UUID 仍不可访问、源程序/测试输入/构建控制隔离、Worker 内部跨域领取、HTTP 作用域及成员停用即时生效。
- 新中间件测试覆盖数据库故障返回 500（不伪装空结果）、失效身份返回 401、私域拒绝及归档域只读。
- 新 schema 的全量 `go -C server vet ./...` / `go -C server test ./... -count=1` 通过；另在独立空库按每个文件一个事务执行 init → down → init，最后正确恢复 35 张表和官方域。
- 本阶段未改 API DTO、生成代码或 UI；尚未开放新域资源前缀，继续等待 owner/用户及 group 协作权限、事务内重新授权与 capabilities 接入。未启动完整服务、未执行外部 Docker/E2E，`server/e2e` 没有 API 环境变量时的跳过不计为端到端通过。

## 2026-09-07 P2b：题目协作权限（进行中）

- 题目所有权与创建记录分离，新增用户/group reader/editor 授权、有效能力响应与协作/转让 API；出题后端不再以站点 admin 作为唯一入口。
- 原生 PostgreSQL 新独立测试库覆盖 owner/reader/editor、撤掉创建能力后保留已有所有权、group 继承与移除个人授权、并发转让、等待域行锁的并发撤权、过时全局 role 声明、HTTP 请求体不能覆盖 owner/域、未授权上传不读取 body。
- 原来在关联查询中以 `problems.author_id` 代替可变所有权的判断已改成当前 owner，避免转让后保留创建者特权；完整 group 继承与关联资源策略仍在 P2 后续。
- Swag/Orval 已同步。生成脚本明确使用本项目安装的 CLI 和 Node，Windows 生成文件偶发 I/O 失败只有限重试格式化；生成/语法错误及持续 I/O 错误仍失败退出。
- mock 记录独立 owner 并随身份切换重算题目能力；owner 可调用自己的 mock 工作台，但不会因此获得站点后台权限。多域角色、group 授权的完整 mock 与能力驱动前端页面尚未完成。
- 本阶段验证：Server 全量 vet/test 在新 PostgreSQL 库通过；题目套件 29 项，包含真实 HTTP/数据库授权和并发测试。UI 24 项测试、类型检查、正常/mock 构建、格式检查通过，正常产物无 mock 运行时标记；重复生成的文件集合与 SHA-256 一致。独立空库 init → down → init 成功恢复 36 张表和官方域。预览恢复在 `127.0.0.1:5173`；未执行完整 Docker/E2E。

## 2026-09-07 P2c：比赛权限核心

- 当前工作区加入 `contests.owner_id` / `admission`、`contest_access` 多角色用户/group 授权，`contest_staff` 改为只读有效赛务视图，按有效域成员和账号计算继承。
- 编辑、预览、赛务读取、答疑/重测、参赛资格/报名/提交分别计算。比赛编辑和题目编排、直接赛务管理、报名、澄清写入以及比赛提交创建已接入域/资源事务检查；裁判回复和标记已答复合并为一个事务。
- 新建独立原生 PostgreSQL 测试库运行全量 Server 测试，发现旧测试错误允许 observer 提交；已调整为观察员拒绝、升级为 jury 后允许非参赛测试提交，提交套件复测通过；`go vet ./...` 通过。尚未完成本阶段完整验收，不据此勾选 P2c 完成。
- 已补管理列表权限过滤、协作/转让/删除 API、admission/owner/capabilities DTO、同域授权主体 FK、密码变动复核、并发转让、group 撤权和 HTTP 封榜/观察员/旧 role 声明回归。全量 Server 测试在新的 PostgreSQL 库通过。
- 比赛详情/赛务读取开始使用后端能力；mock 禁止 jury 授权人员、observer 提交和迟到报名。浏览器实测观察员的只读答疑不显示提问/回复按钮，选手无赛务入口，随后恢复原演示身份与工作台页面。只读答疑不再留空操作栏，加载失败显示可重试错误，不伪装为空列表。
- P2c 当时的剩余项：重测的全站 admin 限制及隐式域资源管理者读取已由 P2d 接入；完整 group mock、多域导航和编辑界面、显式版本发布、题单协作、跨域复制、Worker 域上下文及完整 Docker/E2E 仍未完成。保留整个目标，不以已通过的窄范围测试代替最终验收。
- 本阶段验收：Server 全量 vet/test 在新的 PostgreSQL 库通过；新增 3 项比赛纯策略测试、5 项真实数据库/HTTP 协作测试，覆盖列表隔离、group 撤权、密码变化复核、并发转让、旧 role 声明与封榜/观察员边界。UI 25 项测试、类型/格式检查、正常/mock 构建通过，正常产物无 mock 标记，重复生成文件集合与 SHA-256 一致；独立空库 init/down/init 恢复 36 张表与 `contest_staff` 只读视图。完整 Docker/E2E 未执行。

## 2026-09-07 P2d：提交与重测授权核心

- 移除重测路径的全站 admin 门槛，以当前域和父资源权限限定操作。比赛裁判必须给出比赛边界；题目级选择器无论谁发起都只作用练习，域资源管理者的其他选择器仍受域限制。
- 读取、源码与反馈分别授权；伪造/过时的 admin 声明不再绕过数据库权限。包 reader 可看私题练习记录但不能读他人源码；域资源管理者与比赛赛务可读取相应比赛源码和完整反馈。
- 重新检查 Worker 与重测锁顺序：资源授权 guard 与统计行分离，重测 generation 在域内串行化，队列行先于提交行；真实 PostgreSQL 测试让 Worker 持有 job/submission，确认其仍可更新统计并完成，不与等待重测形成环。
- 控制台按能力展示重测及观察员只读视图；mock 新增比赛限定的批次、模拟改判、generation 与取消恢复，不执行真实代码。完整多域/group mock、题单协作、版本发布及其他原目标工作继续保留。
- 本阶段验收：Windows 原生 `go -C server vet ./...` 与配置真实 PostgreSQL 的 `go -C server test ./... -count=1` 通过；提交套件 46 项，新增 6 项数据库/HTTP/并发回归，最终授权修改后单独复跑这 6 项及 handler 测试通过。UI 28 项测试、类型/格式检查和正常/mock 构建通过；正常产物不含 mock 运行时标记，生成客户端无未提交差异。
- 浏览器实际切换裁判/观察员：裁判有重测操作但无人员授权入口，观察员仅显示记录与改判详情，无发起/取消表单；随后恢复管理员身份和原工作台页面。未创建浏览器重测批次或清除演示数据。完整 Docker/E2E 未执行，`server/e2e` 未配置外部 API 时的跳过不计为端到端通过。

## 2026-09-07 P2e：题单协作核心

- init 加入必填 owner 和用户/group 授权，创建者不随转让变化；题单所有权、编辑、发布可见性、删除及协作能力分别计算，过时的 admin 声明无效。
- 题单与关联题目各自授权；SQL 条目/计数/进度复用题目可见性策略，group 继承动态生效。不可见已有条目阻止整单替换，避免编辑者意外删除自己看不到的数据。题单的行锁取得后才查询协作授权，覆盖等待并发撤权的情形。
- 后端新增协作/转让 API，前端详情提供能力驱动的协作面板，设置与编排分别保存。mock 同页支持用户授权、转让和撤销，以及隐藏条目的保留；完整多域与 group 数据图仍属于 P3，不把当前官方域 mock 当成完整隔离验收。
- 新独立原生 PostgreSQL 测试库的全量 Server vet/test 通过；新增 3 项纯策略、6 项真实数据库/HTTP/并发测试，包含最强授权、group 撤权、并发转让、等待撤权、停用/归档、隐藏条目和跨域外键。最后补充的停用/归档与 FK 错误码断言在题单套件复测通过。
- UI 31 项测试、类型/格式检查及正常/mock 构建通过。Swag/Orval 重复生成的 222 个文件集合及 SHA-256 一致；Windows 生成文件的短暂 I/O 错误由已有有限重试处理，未修改系统配置。独立空库 init → down → init 成功恢复 37 张表、官方域和赛务视图。
- 浏览器创建临时题单后进入 `/problem-sets/4`；授予独立 jury 账号 editor、observer 账号 reader。编辑者可分别保存设置/编排，可见性禁用且无删除按钮；两处未保存的草稿互不覆盖。只读协作者可见题目与授权记录，无修改入口。只删除本轮临时题单，确认原有 3 个题单仍在；恢复管理员身份及工作台预览。完整多域/group、移动端最终矩阵及 Docker/E2E 仍未完成。

## 2026-09-07 P2f：社区父资源权限核心

- 题解/讨论读写统一继承当前域与父题目权限，旧作者不能绕过父资源撤权；有效作者保留可访问内容的编辑权，资源治理者只能删除，不能冒名改写。题解私有草稿对作者和域资源管理者可见，不因拥有父题目而自动公开给题目 owner。
- 列表不读取题解正文，详情 SQL 直接执行防剧透投影；当前练习 AC 才能解锁，点赞及读取/回复讨论同样受限。讨论响应增加 `canPost` 与每条内容权限，前端不再用全局 role 推导编辑/删除。
- 题目/题解均支持楼中楼，事务检查和同线程复合 FK 共同约束；比赛普通讨论从路由、schema、生成契约和 mock 中移除，保留澄清流程。
- 新独立原生 PostgreSQL 库全量 Server vet/test 通过；content 共 29 项，新增 8 项父资源数据库/并发回归，另有跨域 HTTP 测试覆盖旧 admin 声明、域/作者覆盖请求、短编号、只读成员与移除的比赛讨论路由。并发观测按查询前缀匹配，避免 PostgreSQL 的 1 KiB 活动查询截断造成假失败。
- UI 35 项测试、类型/格式及正常/mock 构建通过；mock 按独立账号记录点赞，题目/题解线程使用与正常页面一致的权限响应，评论编号不复用已删除记录。Windows 生成文件的短暂 I/O 错误仍最多重试 3 次，间隔改为 1/2 秒；语法或生成错误不重试。Swag/Orval 重复生成的 223 个文件集合及 SHA-256 一致，正常产物无 mock 运行时标记。
- 浏览器验证管理员可治理删除但无他人内容编辑入口；普通 demo 账号创建临时主楼，独立 jury 账号回复，编辑入口仅出现在各自评论上。随后仅删除这条临时评论链，确认原讨论保留，恢复管理员与工作台。独立空库 init → down → init 恢复 37 张表、官方域，确认已无比赛讨论字段。未执行完整 Docker/E2E；多域/group mock、移动端最终矩阵、版本发布、跨域复制、统计与 Worker 上下文等原目标继续保留。

## 2026-09-07 P2g：发布版本隔离

- 已加入独立 `problem_workspaces` 元信息、工作/数据 revision、候选数据与不可变 `problem_versions`，新增显式发布/版本列表接口。工作题面更新不再写公共 Markdown；原测试数据表仅代表候选。发布核对工作 revision、候选版本和数据 revision，重复发布同一输入幂等。
- 构建输入改为排队时封存的 JSON，领取/重领不读取后续工作修改，并清空旧尝试上传状态。完成仅更新匹配当前 data revision 的候选，不自动发布。题面变更不使数据 revision 增长。
- 管理列表与详情读取工作元信息，公共题库不列出未发布题目；owner/包协作者仍可预览自己的未发布题目，但不能创建未发布版本的评测。比赛编排保留原版本，赛务通过 `expectedVersion` 明确采用新版本；已有提交与 job 不变，重测采用当时目标版本，取消恢复 prior 版本及结果。
- 提交、判题及构建 DTO/Worker 上下文携带发布版本、域与数据修订。引用中的题目拒绝硬删除；未被引用的题目和其版本允许一起删除。原数据库测试夹具显式创建独立 fixture 发布，不再假设创建/上传等同发布。
- 独立原生 PostgreSQL 数据库 `vertex_release_test_20260907` 全量 Server vet/test 通过。新增 7 项发布数据库回归覆盖候选隔离、幂等与不可变版本、封存构建重试、judge generation、比赛固定/采用版本与取消重测、工作元信息查询、标题同步及无引用删除；新增 1 项真实 HTTP 回归覆盖短编号、缺失/非法/超大 body、旧 revision、越权与跨域发布、观察员换版拒绝和成员停用。
- UI 提供发布确认/语言选择/历史与赛务采用版本，提交显示版本；mock 同页模拟候选、发布、固定版本和取消恢复，不解析 ZIP 或执行代码。预览样例使用候选快照，后续未构建修改或过期构建不能替换样例，发布不重置累计提交数。40 项前端测试、类型/格式检查和正常/mock 构建通过。
- 重复 Swag/Orval 生成的 227 个文件集合及 SHA-256 一致；Windows 暂时性生成文件 I/O 错误由已有有限重试恢复，未更改系统配置。正常构建无 mock 运行时标记。新的独立空库 `vertex_release_roundtrip_20260907` 执行 init → down → init，恢复 38 张表及官方域。
- 浏览器新建临时题目：构建完成仍显示「尚未发布」，确认发布才出现 v1；保存第二份题面后公开页仍显示原 v1。随后只删除本轮临时题目，原有 12 个演示题目保留，恢复管理员与出题列表。
- E2E 源码已改为上传/构建后显式发布，并增加未发布读取拒绝、已发布题面隔离和评测版本保留断言；这里只完成编译，未配置 `E2E_BASE_URL` 的跳过不计作端到端通过。当前原生 shell 未找到 Docker CLI，完整 Docker/CI/E2E 尚未执行；不改用 WSL，也不清空原有开发库。
- P2g 以 `7553bbb` 为上一签名基线，按阶段签名且不推送。跨域复制、统计/全部关联查询最终审计、正式多域资源前缀、域导航、完整用户/group 协作界面与 mock、移动端以及 P4 最终验收仍在 Goal 内，不能把本阶段通过当成整个目标完成。

## 2026-09-07 P2h：独立题目复制后端

- 以签名提交 `927c4c3` 为基线，新增 `POST /api/domains/{domain}/problem-copies`：目标由路径决定，源由域 + 数字编号/UUID + 发布版本明确选择。源包 `copy` 与目标 `problem.create` 必须同时有效，站点角色缓存不能越权；账号、排序后的源/目标域与源授权在事务内锁定并复核。
- 副本是新 owner、新域内编号的未发布草稿；题面、标签、全部程序（包括未激活项）、测试计划和候选样例来自所选版本，不夹带后来工作修改。不继承授权、提交、比赛关系或构建任务；来源说明合并保留，详细来源仅供目标包协作者读取。
- 文件复制使用 Go `os.Root` 限定路径，拒绝符号链接/异常条目，验证完整测试对、64 MiB / 4096 条目上限以及手工导入和构建两种现有哈希格式。创建独立普通文件，不使用硬链接。复制前后失败都不留下可用的半成品；明确 SQL 回滚会清理本次新目录，提交结果不明时保留文件，避免误删可能已提交的副本。
- 新独立原生 PostgreSQL 库 `vertex_copy_test_20260907` 的全量 Server vet/test 通过，最后的复制查询优化及补充断言后，相关套件复测通过。新增 10 项 authoring 测试（4 项真实数据库/并发、5 项文件行为、1 项输入校验）和 1 项跨域 HTTP 回归；当前 Windows 实际执行了符号链接拒绝，不是跳过。
- 证据覆盖组继承、等待目标角色/源组撤权后拒绝、归档源可复制/归档目标拒绝、源后续编辑不混入、源删除后副本重新发布和 judge claim 的目标域/路径、禁止来源篡改、重复复制保留说明、坏文件与后续 SQL 失败清理。HTTP 覆盖缺失/超大 body、目标/owner 伪造、跨域 UUID、停用即时失权以及公共 DTO 不泄露私域来源。
- 另一个独立空库 `vertex_copy_roundtrip_20260907` 的 init → down → init 通过，恢复 39 张表、官方域和发布/来源不可变约束。保留此前开发与测试库，不在旧 schema 库上直接运行新代码。
- Swag/Orval 已同步，重复生成的 231 个文件集合和 SHA-256 一致；UI TypeScript、40 项测试、格式检查、正常/mock 构建及产物隔离通过。UI 工作目录的 pnpm 为 11.9.0，仍使用全局 store；未改系统配置。Worker 代码本阶段未改，复制后的领取上下文已在 Server 的真实库测试中验证。
- **产品闭环仍未完成**：复制入口/来源面板随 P3 多域 UI 接入，目前不宣称 mock 已支持跨域复制。下一步优先正式多域资源 API、域导航/切换、角色与 group 界面，并继续统计/关联读策略最终审计、完整多域 mock、移动端、Docker/CI/E2E。未配置外部 API 的 E2E 跳过不计通过；阶段签名，不推送。

## 2026-09-07 P3a：域路由、导航与多域 mock 底座

- 以 `8f4fc6a` 为基线新增 91 条带域资源路径，包含题目/包、比赛/赛务、提交/重测、题单、社区、个人统计与公告读取。复用实际 handler，旧资源路径固定官方域；认证、站点账号及系统队列治理保持全局。标签/公告治理的域能力接口仍待拆分。
- Swag 同时生成新旧路径；新增断言核对每条路径的精确参数集合。真实 PostgreSQL 的作用域测试改用生产 `httpapi.Router`，验证短编号先按路径域解析、外域 UUID 拒绝，以及成员停用后多个领域入口都被拒绝。
- 前端 31 个页面/组件改用明确的域绑定 API 与导航；域/账号变化重挂载，读取中止与响应序号保护继续生效。旧页面规范化官方域，切换仅保留栏目，登录返回与比赛题目的 canonical 跳转保留当前域。出题导航不再限站点 admin，创建/提交等入口读取域能力，资源写权限仍由后端决定。
- mock 内置官方、训练与私有域，资源图/编号计数/评测状态各自保存，账号身份共享但全局 role 不变；同号题目与外域 UUID 不混用。普通 demo 在训练域可出题，在官方没有创建权限；只读成员不能创建/提交，停用和归档立即阻止相应请求。补比赛创建/设置/编排的模拟，保留已有绑定版本并拒绝外域题目。
- Server 全量 vet/test 在现有新 schema 的原生 PostgreSQL 独立测试库通过；前端 47 项测试及类型/格式检查通过。浏览器实际创建训练域临时题目、保存草稿、从详情切到官方列表并确认创建按钮禁用；私域拒绝后仍能切换身份。仅删除本轮临时题目，保留原官方数据，恢复管理员与官方工作台。
- 正常与 mock 构建通过，生产产物不包含 mock 运行时标记；Swag/Orval 重复生成的 271 个文件集合及 SHA-256 一致。Windows 短暂生成文件 I/O 错误通过原有有限重试恢复，未改系统配置。构建仍有原有 CodeMirror 大块提示；移动端几何与完整 Docker/E2E 未在本阶段执行。
- 本阶段不是整个 P3 完成：域创建与成员/角色/group 页面、完整资源协作/只读控制、复制与来源 UI/mock、比赛详情工作流、全部统计策略、移动端及 Docker/E2E 仍保留在目标中。后续不再以全局 role 替代域/资源权限，也不以当前默认三域模拟替代完整矩阵验收。

## 2026-09-07 P3b：域与群组治理页面

- 基线为签名提交 `38d6bb4`。新增域目录/创建/加入与邀请处理，域概览、成员、角色、群组列表及详情页面；群组编辑、成员维护、转让与删除只在详情进行。内置角色不可修改，owner 不能通过成员设置降级/停用，能力由现有生成契约驱动。
- `DomainResponse` 增加 `canArchive`，归档后保留有权恢复者的入口，不授予转让或一般写权限。无权进入的邀请/申请/停用状态明确提示，域目录保留加入动作；目录按账号重新挂载，异步读取可取消，迟到的确认/创建响应不能操作旧页面。
- mock 增加域创建、可见性/加入策略、审批与邀请、域转让、归档恢复、内置/自定义角色、成员状态、群组 CRUD/成员和所有权规则。新域从空数据开始，组编号不复用，账号全局身份不改写。组管理者不能越权管理域或转让/删除组；实际资源 group 授权继承仍待下一阶段完成。
- Server 全量 vet/test 使用原生 PostgreSQL 独立测试库通过；新增归档恢复能力回归。前端新增 6 项治理模拟回归，总计 53 项测试及类型/格式检查通过，正常/mock 构建通过且产物隔离。未新增 migration 或改动已有开发数据库。
- Swag/Orval 重复生成的 271 个文件集合与 SHA-256 一致；Windows 临时文件 I/O 失败由原有有限重试恢复，未改系统设置。UI 的组设置在成员刷新时保留未保存字段；邀请/审核/停用提示不伪装为数据为空，成功写入后的域状态刷新错误单独显示。完整 Docker/E2E 和移动端几何本阶段未执行。
- 浏览器：普通 demo 创建私有 `workflow-qa-20260907`「治理流程验收」域，新增 jury 普通成员及 observer 只读邀请；创建临时组并让 jury 作为组管理者保存设置，确认无转让/删除入口；observer 接受邀请后可只读组信息但不能读取未加入组的成员。自定义角色创建/删除通过，临时群组已删除。域归档后设置禁用且恢复入口保留，恢复验证后最终归档；该演示域和成员关系按事先说明保留，未重置原官方/训练数据。
- 仍未完成：题目/比赛完整协作面板与 group 继承 mock、复制/来源 UI、比赛详情化、域标签/公告治理、统计读策略、移动端几何与完整 Docker/CI/E2E。本阶段不作为整个 Goal 完成声明。
