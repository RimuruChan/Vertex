// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: rankboard.sql

package dbgen

import (
	"context"
	"time"
)

const listRankboardCells = `-- name: ListRankboardCells :many
SELECT user_id, problem_id, attempts, penalty_sec, score, solved_at,
		        public_attempts, public_penalty_sec, public_score, public_solved_at,
		        pending_count, last_submit_at
		 FROM contest_submission_cells WHERE contest_submission_cells.contest_id = $1::uuid
`

type ListRankboardCellsRow struct {
	UserID           string
	ProblemID        string
	Attempts         int
	PenaltySec       int
	Score            int
	SolvedAt         *time.Time
	PublicAttempts   int
	PublicPenaltySec int
	PublicScore      int
	PublicSolvedAt   *time.Time
	PendingCount     int
	LastSubmitAt     *time.Time
}

func (q *Queries) ListRankboardCells(ctx context.Context, contestID string) ([]ListRankboardCellsRow, error) {
	rows, err := q.db.QueryContext(ctx, listRankboardCells, contestID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRankboardCellsRow{}
	for rows.Next() {
		var i ListRankboardCellsRow
		if err := rows.Scan(
			&i.UserID,
			&i.ProblemID,
			&i.Attempts,
			&i.PenaltySec,
			&i.Score,
			&i.SolvedAt,
			&i.PublicAttempts,
			&i.PublicPenaltySec,
			&i.PublicScore,
			&i.PublicSolvedAt,
			&i.PendingCount,
			&i.LastSubmitAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRankboardParticipants = `-- name: ListRankboardParticipants :many
SELECT participant.user_id, u.username
		 FROM contest_participants AS participant
		 JOIN users u ON u.id = participant.user_id
		 WHERE participant.contest_id = $1::uuid
`

type ListRankboardParticipantsRow struct {
	UserID   string
	Username string
}

func (q *Queries) ListRankboardParticipants(ctx context.Context, contestID string) ([]ListRankboardParticipantsRow, error) {
	rows, err := q.db.QueryContext(ctx, listRankboardParticipants, contestID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRankboardParticipantsRow{}
	for rows.Next() {
		var i ListRankboardParticipantsRow
		if err := rows.Scan(&i.UserID, &i.Username); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
